public enum ChromeScripting: String {
    case application = "application"
    case bookmarkFolder = "bookmark folder"
    case bookmarkItem = "bookmark item"
    case tab = "tab"
    case window = "window"
}
